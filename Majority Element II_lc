class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        mapp = Counter(nums)
        ans =[]
        for key in mapp: 
            if mapp[key] > len(nums)//3: 
                ans.append(key)
        return ans 
#1, 1m
